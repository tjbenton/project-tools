{
  "name": "${project_name}",
  "version": "0.0.0",
  "description": "Contains all the projects for ${project_name}",
  "scripts": {
    "test": "make ci-test"
  },
  "author": "${author_name} <${author_email}>",
  "license": "ISC",
  "devDependencies": {
    "lint-rules": "github:ma-shop/lint-rules",
    "project-tools": "^${version}"
  }
}
